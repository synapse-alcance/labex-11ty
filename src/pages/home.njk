---
title: Labex | Home
permalink: "{{ site.baseurl }}/"
cssSheets: ["home", "buttons", "sections", "cards", "results"]
jsScripts: ["home"]
---
{% extends "layouts/base.njk" %}
{% block content %}

  <section class="hero">
    <video class="hero-video" autoplay muted loop playsinline>
      <source src="{{ baseurl }}/vid/labex-intro-video.mp4" type="video/mp4">
    </video>
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <h1>Tecnologia avançada, cuidado que acolhe</h1>
      <p>Equipamentos de última geração e uma equipe preparada para receber você.</p>
      <a href="{{ site.info.waUrl }}" class="btn btn-primary">Agendar</a>
    </div>
  </section>

  {% include "partials/results.njk" %}

  <section class="section examinations">
    <div class="container">
      <div class="section-header">
        <h1>{{ examinations.title }}</h1>
      </div>
      {% for subsection in examinations.subsections %}
      <div class="section-content">
        <h2>{{ subsection.title }}</h2>
        {% for paragraph in subsection.text %}
        <p>{{ paragraph }}</p>
        {% endfor %}
        <img src="{{ baseurl }}{{ subsection.imgSrc }}" alt="{{ subsection.imgAlt }}">
        <h3>Exames disponíveis:</h3>
        <ul>
          {% for item in subsection.list %}
          <li>{{ item }}</li>
          {% endfor %}
        </ul>
      </div>
      {% endfor %}
      <div class="section-cta">
        <a class="btn btn-primary" href="{{ examinations.ctaPath }}">{{ examinations.ctaText }}</a>
      </div>
    </div>
  </section>

  <section class="section plans">
    <div class="container">
      <div class="section-header">
        <h1>{{ plans.title }}</h1>
      </div>
      <div class="section-content">
        {% for paragraph in plans.intro %}
        <p>{{ paragraph }}</p>
        {% endfor %}
        <div class="plans-slider">
          <div class="plans-track">
            {% for company in plans.companies %}
            <div class="plans-slide">
              <img src="{{ baseurl }}{{ company.imgSrc }}" alt="{{ company.imgAlt }}">
            </div>
            {% endfor %}
            {% for company in plans.companies %}
            <div class="plans-slide">
              <img src="{{ baseurl }}{{ company.imgSrc }}" alt="{{ company.imgAlt }}">
            </div>
            {% endfor %}
          </div>
        </div>
        {% for paragraph in plans.text %}
        <p>{{ paragraph }}</p>
        {% endfor %}
      </div>
      <div class="section-cta">
        <a class="btn btn-primary" href="{{ plans.ctaPath }}">{{ plans.ctaText }}</a>
      </div>
    </div>
  </section>

  <section class="section differentiators">
    <div class="container">
      <div class="section-header">
        <h1>{{ differentiators.title }}</h1>
      </div>
      <div class="section-content">
        {% for paragraph in differentiators.intro %}
        <p>{{ paragraph }}</p>
        {% endfor %}
        <div class="differentiators-cards">
          {% for card in differentiators.cards %}
          <div class="card fade-up">
            <div class="differentiators-card-top">
              <div class="differentiators-card-icon pop">
                <img src="{{ baseurl }}{{ card.imgSrc }}" alt="{{ card.imgAlt }}">
              </div>
              <div class="differentiators-card-title">
                <h3 class="card-title">{{ card.title }}</h3>
              </div>
            </div>
            <div class="differentiators-card-bottom">
              <p class="card-text">{{ card.text }}</p>
            </div>
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>

  <section class="section testimonials">
    <div class="container">
      <div class="section-header">
        <h1>{{ testimonials.title }}</h1>
      </div>
      <div class="section-content">
        {% for paragraph in testimonials.intro %}
        <p>{{ paragraph }}</p>
        {% endfor %}
        <div class="testimonials-quotes">
          {% for quote in testimonials.quotes %}
            <div class="testimonials-quote">
              <div class="testimonials-quote-vignette">
                <div class="testimonials-quote-left">
                  <img src="{{ baseurl }}/icon/quote-left-solid-full.svg" alt="Ícone de aspas duplas à esquerda.">
                </div>
                <div class="testimonials-quote-text">
                  {{ quote.text }}
                  <img src="{{ baseurl }}/icon/quote-right-solid-full.svg" alt="Ícone de aspas duplas à direita.">
                </div>
              </div>
              <div class="testimonials-quote-authorship">
                {{ quote.author }}
                <span class="testimonials-quote-date">({{ quote.date }})</span>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
      <div class="section-cta">
        <a class="btn btn-primary" href="{{ testimonials.ctaPath }}">{{ testimonials.ctaText }}</a>
      </div>
    </div>
  </section>

{% endblock %}
